<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="freeChatMessage">
 	
	<insert id="insert">
 		insert into free_chat_message(messageNo, member_id, room_name, message_body) 
 		values(free_chat_message_seq.nextVal, #{memberId}, #{roomName}, #{messageBody})
 	</insert>
 </mapper>