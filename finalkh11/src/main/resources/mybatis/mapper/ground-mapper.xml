<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="ground">

 	<select id="selectList" resultType="GroundDto">
 		select * from ground where instr(ground_name, #{term})>0 or instr(ground_basic_addr, #{term})>0
 								or instr(ground_detail_addr, #{term})>0
 	</select>
 </mapper>