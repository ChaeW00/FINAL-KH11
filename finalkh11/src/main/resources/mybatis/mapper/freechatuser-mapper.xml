<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="freeChatUser">

 	<insert id="insert">
 		insert into free_chat_user(room_name, member_id) values(#{dto.roomName}, #{dto.memberId})
 	</insert>
 	
 	<select id="selectOne" resultType="FreeChatUserDto">
 		select * from free_chat_user where room_name = #{dto.roomName} and member_id = #{dto.memberId}
 	</select>
 	
 	<delete id="delete">
 		delete free_chat_user where member_id = #{dto.memberId} and room_name = #{dto.roomName}
 	</delete>
 </mapper>